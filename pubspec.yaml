name: multi_account_authentication
description: Simple simultaneous authentication of multiple accounts
version: 1.0.4
homepage: https://github.com/lowski/multi_account_authentication

environment:
  sdk: ">=3.0.3 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  googleapis: ^13.2.0
  googleapis_auth: ^1.4.1
  http: ^1.1.0
  flutter_appauth: ^6.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
flutter:
