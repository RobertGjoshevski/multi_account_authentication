name: multi_identity_authentication
description: A new Flutter package project.
version: 1.0.0
homepage: https://github.com/lowski/multi_identity_authentication
publish_to: none

environment:
  sdk: ">=3.0.3 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  googleapis: ^11.4.0
  googleapis_auth: ^1.4.1
  http: ^1.1.0
  flutter_appauth: ^6.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
flutter:
